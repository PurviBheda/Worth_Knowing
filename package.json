{
  "name": "worth-knowing-platform",
  "version": "1.0.0",
  "description": "Root workspace script runner for Worth Knowing Digital Newspaper",
  "private": true,
  "scripts": {
    "install:all": "npm install && npm install --prefix frontend && npm install --prefix backend",
    "dev": "concurrently --kill-others \"npm run dev --prefix frontend\" \"npm run dev --prefix backend\"",
    "build:all": "npm run build --prefix frontend && npm run build --prefix backend",
    "db:generate": "npm run prisma:generate --prefix backend",
    "db:migrate": "npm run prisma:migrate --prefix backend",
    "db:seed": "npm run prisma:seed --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
